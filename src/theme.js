import { extendTheme } from '@chakra-ui/react';

const customTheme = extendTheme({
  colors: {
    brand: {
      50: '#000000',
      100: '#7baaf7',
      200: '#333333',
      300: '#FFFFFF',
      400: '#7baaf7',
      
    },
  },
});

export default customTheme;